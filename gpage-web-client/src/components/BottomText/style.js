import styled from 'styled-components';

import theme from '../../theme';

const BottomTextStyle = styled.p`
  font-size: 14px;
  text-align: center;
  padding: ${theme.size.space}px;
`;

// eslint-disable-next-line import/prefer-default-export
export { BottomTextStyle };
