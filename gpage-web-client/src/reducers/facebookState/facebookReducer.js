const initialState = {
  loading: true,
  pages: [],
  conversations: [],
  iniData: false,
  conversation: null,
  newMessage: null
};

const facebookReducer = (state = initialState, action) => {
  switch (action.paylaod) {
    default:
      return state;
  }
};

export default facebookReducer;
