import React from 'react';

import { DefaultLayout } from '../../layout';

const title = 'Hình thức thanh toán';

const Payments = () => {
  return <DefaultLayout title={title}>Updating...</DefaultLayout>;
};

export default Payments;
