import styled from 'styled-components';
import theme from '../../theme';

const WrapShip = styled.div`
  padding: ${theme.size.space}px ${theme.size.space * 2}px
    ${theme.size.space * 2}px ${theme.size.space * 2}px;
`;

export { WrapShip };
